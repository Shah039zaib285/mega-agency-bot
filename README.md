Mega Agency Bot
ENV:
  SESSION_NAME - unique session id
  WHATSAPP_BRIDGE_URL - bridge public URL
  CALLBACK_PORT - port for receiving send commands (default 3001)
  CALLBACK_BASE - public base URL (if behind proxy)
Start: npm install && npm start
